<%= form_with(model: @goal_sheet, url: staff_goal_sheets_edit_path, local: true) do |f| %>
    <%= render partial: "form", locals: { f: f } %>
    <%= f.submit "更新", class: "btn btn-outline-success" %>
<% end %> 