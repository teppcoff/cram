<div class="body-title">
    <h5>テスト結果登録</h5>
</div>
<div class="content">
    <%= form_with model: @score_sheet, url: student_score_sheets_path, local: true do |f| %>
        <%= render "layouts/error_messages", model: f.object %>
        <%= render "form", f: f %>
        <div class="form-submit">
            <%= f.submit "登録", class: "btn btn-outline-primary" %>
        </div>
    <% end %>
</div>